<a href="/links/new"><h6>New link</h6></a>
<a href="/links/new"><h6>New link</h6></a>
<a href="/links/new"><h6>New link</h6></a>
<h6><%= current_user.email %></h6>
<h1>Your links</h1>

<table>
  <tr>
    <th>Slug</th>
    <th>Target URL</th>
    <th>Created At</th>
  </tr>
  
  <% if @links %>
    <% @links.each do |link| %>
      <tr>
        <td><%= link.slug %></td>
        <td><%= link.target_url %></td>
        <td><%= link.created_at %></td>
        <td><a href="/links/<%= link.id %>/edit">Edit</a></td>
        <td><a href="/links/<%= link.id %>/delete">Delete</a></td>
      </tr>
    <% end %>
  <% else %>
    <h1>Add some links!</h1>
  <% end %>
  

<table>
